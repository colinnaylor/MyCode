#ifndef SECRETS_H
#define SECRETS_H

// Copy this file to Secrets.h and fill in real values.
// Secrets.h is gitignored and will not be committed.

// Wi-Fi networks to try, in order.
constexpr const char* WIFI_SSIDS[] =
{
    "your-ssid-1",
    "your-ssid-2"
};

constexpr const char* WIFI_PASSWORDS[] =
{
    "your-password-1",
    "your-password-2"
};

constexpr int WIFI_NETWORK_COUNT = 2;

// OTA update password.
constexpr char OTA_PASSWORD[] = "change-me";

// Cloud upload bearer token.
constexpr char CLOUD_UPLOAD_TOKEN[] = "change-me";

#endif
